# Copy to `.env` in the repo root. Expo injects EXPO_PUBLIC_* at build time.
# Web / simulator on same machine:
EXPO_PUBLIC_API_BASE_URL=http://localhost:3000
# Physical device on LAN — use your PC's IP and ensure Nest is reachable:
# EXPO_PUBLIC_API_BASE_URL=http://192.168.1.10:3000
